# Database Configuration
DB_NAME=u354738377_deaconschool
DB_USER=u354738377_deaconschool
DB_PASSWORD=@6pv7BOIvC
DB_HOST=92.113.22.53
DB_PORT=3306
DB_DIALECT=mysql
NODE_ENV=development
CORS_ORIGIN=*
   JWT_SECRET=your_jwt_secret_key

# App
# NODE_ENV=production
PORT=3000
# VERCEL=1


#    NODE_ENV=production

# ====== Local development only ======
# Run the server with nodemon or node locally
# NODE_ENV=development
# Do NOT set VERCEL locally (or set VERCEL=0)
# VERCEL=0
# Local port (optional; defaults to 3000)
# PORT=3000
# Frontend origin during local dev (adjust if needed)
# CORS_ORIGIN=http://localhost:5173

# ====== Vercel (deployment) only ======
# On Vercel, set these in Project → Settings → Environment Variables
# Use production mode on Vercel
# NODE_ENV=production
# Vercel sets process.env.VERCEL automatically; you do NOT need to set it
# VERCEL=1
# Your deployed frontend URL(s)
# Single origin:
# CORS_ORIGIN=https://your-frontend-domain.vercel.app
# Multiple origins (comma-separated):
# CORS_ORIGIN=https://your-frontend-domain.vercel.app,https://your-custom-domain.com